# Shared Package

Planned home for shared types and utilities between the web app and worker.

Not in use yet. Currently, shared types live in `apps/web/src/lib/types.ts`. Once the worker needs to share TypeScript types or validation schemas with the web app, they'll move here.
